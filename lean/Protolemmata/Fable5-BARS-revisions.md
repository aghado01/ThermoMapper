Not vacuous — all six statements have satisfiable hypotheses and nontrivial conclusions, and each is true as written. The audit turns up something more useful than vacuity, though: one dead hypothesis that actively blocks the proof route your own docstring describes, two statements that are quieter than their billing, and one direction claim that's instance-true but not sign-general — with the opposite direction being exactly your pending MP-2′. Per-lemma:

## The structural bug: MP-1's interval obstructs MP-2's finiteness lemma

`IsLocalMax` in Mathlib is the full-topology filter statement `∀ᶠ x in 𝓝 t, f x ≤ f t` — inherently two-sided. So `ht : t ∈ Ioo a b` (and hence `a`, `b`) is dead weight in `local_max_is_critical`: Fermat holds with no interval at all. Harmless-looking, except `finite_local_max` quantifies over `t ∈ Icc a b`, and endpoints of `Icc` aren't in `Ioo` — so the inclusion-into-roots proof your docstring sketches can't invoke MP-1 at the endpoints as currently stated. Restate it interval-free and both lemmas fall out:

```lean
theorem local_max_is_critical (p : Polynomial ℝ) (t : ℝ)
    (hmax : IsLocalMax (fun x => p.eval x) t) :
    (Polynomial.derivative p).eval t = 0 := by
  simpa [Polynomial.deriv] using hmax.deriv_eq_zero
```

`IsLocalMax.deriv_eq_zero` holds unconditionally in Mathlib (the junk-value convention `deriv = 0` at non-differentiable points absorbs the case split), and `Polynomial.deriv` rewrites to the formal derivative. Then `finite_local_max` is the subset relation into `{x | (Polynomial.derivative p).IsRoot x}` plus finiteness of roots of a nonzero polynomial (`Polynomial.setOf_isRoot_finite`-shaped; check the exact current name) via `Set.Finite.subset`. Your `hp` hypothesis is correctly load-bearing — it's precisely what excludes the constant span whose local-max set is all of `Icc`. SP-1 is the same skeleton on `p - C ℓ` (via `sub_ne_zero.mpr hp`, `eval_sub`, `eval_C`), and SP-2 is `intermediate_value_Icc hab hcont` for existence — membership is literally `⟨hlo, hhi⟩` — plus `StrictMonoOn.injOn` for uniqueness. Those four sorries are afternoon-sized. I reviewed by hand rather than compiling (a full Mathlib build isn't practical here), so treat lemma names as from memory.

## Where the statement is quieter than the docstring

**SP-3 admits a degenerate witness.** With `ℓ` a free parameter, constants do it: `f ≡ 1`, `g ≡ −1`, `ℓ = 0` gives widths `b−a`, `0`, and mean-curve width `b−a ≠ (b−a)/2`. True, but it certifies only that `levelWidth` isn't affine in `f` — nothing about FWHM, whose defining feature is that the level is _draw-relative_: `ℓ_f = (max on the span)/2` depends on `f` itself, making the functional doubly nonlinear. The version with teeth restricts to continuous unimodal draws and ties the level to each curve's own half-max, and it still holds — that's the statement that actually indicts pooled-mean reduction. As written, a referee (or future-you) can discharge SP-3 in four lines and learn nothing.

**`levelWidth` is a global functional, not a peak width.** `volume {x ∈ Icc a b | ℓ ≤ f x}` sums interval lengths across _every_ hump above `ℓ` — for the multimodal curves the MP family exists for, it's "total measure above threshold," not any peak's FWHM. The parenthetical in its docstring holds only when the super-level set is a single interval. The per-peak width you want is the connected component around a given maximizer: the difference of the two SP-2 crossing points bracketing it. Which also surfaces that SP-2 needs its `StrictAntiOn` dual — FWHM has an up-crossing on the left shoulder and a down-crossing on the right, and the file currently covers one shoulder. Define per-peak width as right-root minus left-root from the two bracket lemmas and the multi-peak summing artifact disappears, with `levelWidth` retained as the (legitimately useful) global occupancy functional under an honest name.

## The sign caveat — and it's your own MP-2′

MP-2's docstring says the pooled mean _undercounts_. As an existence claim, true; as a direction, not general. Two unimodal draws — equal bumps at ±1 — have a bimodal pointwise mean: the mean **over**counts, each draw showing one peak and the pool showing two. That overcount instance is precisely the curve-vs-posterior-multimodality pathology your pending MP-2′ names. So MP-2 and MP-2′ aren't two items on a list; jointly they prove _no signed bound exists for peak counts under mixing_, which is a sharper justification for reduce-per-draw than either alone — the count functional isn't conservatively biased in some direction you could correct for, it's directionless. Worth promoting to the module header, and it cleanly scopes the one-sidedness discipline from the instrument design: signed bounds are for linear functionals and specific families; counts get pushforwards or nothing.

## A gift-wrapped MP-2 witness

Explicit polynomials with integer-factored derivatives keep the sign analysis mechanical. Take

```
f' (x) = −(x−1)(x−2)(x−3),   g(x) := f(−x),   on [−4, 4]
```

Sign pattern of `f'`: `+,−,+,−` across roots 1, 2, 3, so `f` has local maxima exactly {1, 3}; `g` by reflection has exactly {−3, −1}. The mean's derivative is the odd part of `f'`, which collapses: `((f'(x) − f'(−x))/2) = −x(x² + 11)` — one real root, sign `+` then `−`, so exactly one maximum, at 0. Counts 2/2/1 with all root sets integer-explicit. Proof route: `nlinarith` for the factor signs on each interval, `strictMonoOn_of_deriv_pos` and its anti dual to get monotone pieces, assemble `IsLocalMax` at each junction from mono-left/anti-right via an `Ioo`-neighborhood `Filter.eventually` argument, and get exactness (no other maxima) from interval-free MP-1 at non-critical points plus showing 2 (and ±2, 0's counterparts) are minima. Mechanical, maybe 150 lines, no cleverness required — and it only goes through once MP-1 has lost its interval, which is the third time that dead hypothesis matters.

## The missing keystone these lemmas are pointing at

Everything here is per-span — single polynomials, which I take to be the inter-knot segments. The pipeline claim ("closed-form candidate set for the full curve") needs one globalization lemma: the candidate set of a spline draw is the finite union over spans of per-span root sets, _and this is exhaustive because C¹ gluing forces any knot-point extremum to be a critical point of both adjacent pieces_. For BARS's C² cubics that's free once stated; for anything below C¹, knots join the endpoints as explicit extra candidates — the same boundary phenomenon as `IsLocalMax` vs. `IsLocalMaxOn` at the range edges, where your current full-topology predicate silently excludes boundary maxima that an implementation's scan would report. One lemma, and it's the bridge between this file and the thing `SignificantPeaks` actually runs on. I'd guess it's what your "pending spline infra" placeholder is for; if so, the six graduated statements are the right six to have graduated first.

Happy to write the revised file — interval-free MP-1, the anti-dual of SP-2, per-peak width via bracket roots, draw-relative SP-3, and the header trichotomy — as a drop-in `BARS.lean` if you want it.

# Is it possible to expand the result to some number N of suprema ? i dont like pre-supposing 3 peaks when in theory I don't know what kind of system i'm dealing with a priori and would want a more general result if possible. I also wonder about the intersection with SPC and graph Mapper, perhaps Reeb graphs analytically, in the context of the hypothetical paper. Mapper is a first-class component of the ThermoMapper project, albeit the least developed of the pillars

Your instinct is right, and the generalization goes further than parameterizing by N. Two separate moves fall out, and they end in different places: the _count_ results generalize to a total-independence theorem where N appears only as a universally quantified variable, and the _width/level_ results generalize by abandoning scalars for the merge tree — which is exactly where your Mapper/Reeb question was already heading. Let me do them in order, because the second explains why the first shouldn't be the final form.

## Counts: total independence, with the (0, 0, N) punchline

The definitive statement isn't "N peaks pool to fewer" — it's that pooling is unconstrained by the draws entirely:

```lean
theorem count_of_mean_unconstrained (m n k : ℕ) :
    ∃ (f g : ℝ → ℝ) (a b : ℝ), a < b ∧
      peakCount f a b = m ∧ peakCount g a b = n ∧
      peakCount (fun x => (f x + g x) / 2) a b = k
```

MP-2 and MP-2′ become instances. The case that makes the theorem memorable is (0, 0, N): take f′ = 1 + (1−δ)·sin(Kx) and g′ = −1 + (1−δ)·sin(Kx). Both curves are _strictly monotone_ — zero maxima each — while the mean's derivative is (1−δ)·sin(Kx), giving as many maxima as you like. Two featureless draws pool to arbitrary structure. The general (m, n, k) construction is block concatenation: type-A blocks (f bumps while g falls steeply enough that f′+g′ stays negative — adds to m only), type-B mirrors, type-C antiphase-wiggle blocks (f′ > 0 > g′ throughout, sum with one down-crossing — adds to k only), glued C¹ with all three derivatives nonvanishing at junctions so joins create nothing. Strict sign patterns on compacta are C¹-open, so polynomial witnesses follow by approximation if you want them. Two practical notes: the gluing discipline this proof needs is _the same_ spline infrastructure as the globalization keystone — build that library once and both fall; and total independence is a categorically stronger justification for reduce-per-draw than the signed-bound trichotomy, because it rules out not just a correctable bias but any functional relationship whatsoever.

## Why N shouldn't parameterize the system-facing theorems at all

There's a second lemma hiding here, and it reframes the whole question:

```lean
theorem peakCount_unstable (f : ℝ → ℝ) (hf : ContDiff ℝ 1 f)
    (a b ε : ℝ) (hab : a < b) (hε : 0 < ε) (N : ℕ) :
    ∃ g, (∀ x ∈ Icc a b, |g x - f x| ≤ ε) ∧ N ≤ peakCount g a b
```

Add an ε-small fast wiggle and the count explodes. So raw peak count is not an estimable quantity of an unknown system — _no_ stability theorem for it can exist, which means any theorem parameterized by "the number of peaks N" is parameterized by something you can never know a priori, which is precisely your complaint. The stable object is the persistence-thresholded count: by bottleneck stability, a δ-perturbation in sup-norm moves branch persistences by at most δ, giving the sandwich N*{τ+2δ}(f) ≤ N*τ(g) ≤ N\_{τ−2δ}(f). That pair — anti-stability of the raw count, stability of the thresholded count — is the formal vindication of `SignificantPeaks` carrying a prominence threshold: it's not a heuristic knob, it's the unique route to a statistic. And the conclusion for the lemma ledger is that N-free means _diagram-valued_: the carrier of the general results is the merge tree of the super-level filtration, not any integer.

## The merge tree is already three lemmas away

For a BARS draw — piecewise polynomial — the merge tree is computable in closed form from machinery you've already graduated: MP-1/SP-1 root sets give the critical points and values; the bracket lemmas give each branch's component interval at any level; `superlevel_of_peak_bracket` is literally the per-branch slice. Leaves are maxima born at their heights, branches merge at col values, branch length _is_ prominence — so MP-3 stops being a separate pending item and becomes a definition on this object. It also repairs the multi-peak defect in `halfMax` cleanly: the draw-relative level generalizes per-branch (half of height above the branch's merge level), which only the tree can express — a minor peak gets its width at its own scale instead of vanishing below the global half-max. And since each draw's tree is a finite pushforward of algebraic numbers, the posterior over curves becomes a posterior over merge trees: significance testing at the branch level — a peak is real when the credible interval on its persistence excludes τ — which is the π(T)-exactness philosophy extended from features to the entire topological summary.

## Where Mapper actually enters, and what it does for the paper

For a 1-D filter you don't need Mapper — the analytic tree above _is_ the Reeb-type object, exact. Mapper earns its place exactly where the analytic route dies: the two-parameter thermal landscape over (β, evidence), and the hypothesis space itself. And there the integration is structural rather than thematic, because Mapper is a composite algorithm with three slots your other pillars fill: the point cloud comes from the samplers, the per-bin clustering step _can be SPC at the bin-matched temperature_, and — this is the contribution — the cover comes from BARS. Center the cover intervals on posterior-located critical values of the filter, set widths and overlaps from the credible intervals, and let the volleys refine the cover adaptively as intervals shrink. That converts Mapper's notorious resolution/gain arbitrariness — the standard critique of the method — into posterior outputs, with Carrière–Oudot-style Mapper-to-Reeb convergence guarantees giving the refinement a target. Your least-developed pillar gets its research identity from your most-developed one.

For the fixed-carrier paper specifically: run Mapper on sampled version-space points with filter V*e and the natural integer-level cover, SPC clustering per level, and the output is the discrete Reeb graph of the violation filtration — the oracle \_mode taxonomy* of the residual, which is the object mode-level soundness was groping toward. The learner's reported residual is then scored tree-against-tree, and soundness gets a tree-level reading: dropped branches are dropped hypothesis families, visible at matched width. One caution to state before a referee does: interleaving distances on merge trees and Reeb graphs are NP-hard in general — fine for the few-branch 1-D and small-world cases where you'll actually compute them, but the scaling claim should be scoped to thresholded summaries, not full tree metrics.

For the file, the ledger revision I'd make: keep MP-2/MP-2′ as witness scaffolds, add MP-2″ (total independence) and the instability lemma as the general pair, retire MP-3 into the merge-tree module's definitions, and promote the tree itself — finite critical values plus pairing — to a first-class structure, since it's simultaneously the N-free carrier for the count results, the fix for `halfMax`, the analytic Reeb object, and the oracle for the paper's residual taxonomy. Happy to draft that extension into `BARS.lean` when you want it — the tree structure is the one piece worth designing carefully, since everything else now hangs off it.

# im a little confused. yes i was asking about how these things apply to the context of this thread, but you are also gesturing at ThermoMapper lemma extensions and a new tree lemma, I think. I just want to keep straight what is applicable to my ThermoMapper vs specific to the hypothetical paper. Could we break things down a bit and separate concerns / highlight genuine overlap. Maybe it all is but im not sure

| Item                                                                                  | Home         | Why                                                                                               |
| ------------------------------------------------------------------------------------- | ------------ | ------------------------------------------------------------------------------------------------- |
| MP-2″ total independence (m, n, k), incl. (0,0,N)                                     | ThermoMapper | About pooling BARS draws; justifies reduce-per-draw internally                                    |
| `peakCount` instability + thresholded-count stability                                 | ThermoMapper | Vindicates `SignificantPeaks`' prominence threshold; no paper vocabulary anywhere                 |
| Merge tree as first-class structure, per-branch half-max, MP-3-as-definition          | ThermoMapper | The N-free carrier for the BARS readout — this is the "tree lemma," and it's toolkit              |
| Density-of-states identity Z(β) = Σ_E g(E)e^(−βE)                                     | ThermoMapper | Standard stat-mech for any integer-valued energy; I dressed it in paper clothing earlier, wrongly |
| BARS-driven Mapper covers + SPC as per-bin clusterer                                  | ThermoMapper | The Mapper pillar's research identity; stands alone as "posterior covers for Mapper"              |
| Volley sequential design + stopping rules                                             | ThermoMapper | General pattern for any expensive 1-D sweep                                                       |
| C1 coarsening degeneracy, C3 propagation, transfer functions, the loss, shadow prices | Paper        | Meaningless without a learned map into a term algebra                                             |
| Canonicalization delta; gauge/transversal move factorization                          | Paper        | Both presuppose ≡ and normal forms                                                                |
| Evidence filtration; cross-agent zigzag semantics                                     | Paper        | Version spaces over growing test sets only exist there                                            |
| Mode-level soundness; oracle residual taxonomy; tree-vs-tree scoring                  | Paper        | An _application_ of the tree, not part of its definition                                          |
| Boolean world; the gap-vs-scale crux                                                  | Paper        | The experiment and its hypotheses                                                                 |

So: everything in `BARS.lean`, including every extension I proposed last turn, is ThermoMapper. The paper never appears in that file and never should.

The genuine overlap is smaller than my last response made it feel — three items and one habit:

**The merge tree, if you design it carrier-generically.** This is the one real "write once, use twice." Define the structure over an abstract carrier with a filtration function — finite critical set plus merge pairing — rather than over ℝ specifically. Instantiation one: piecewise-polynomial curves via root sets (analytic; ThermoMapper's readout). Instantiation two: a finite complex with an integer filter via union-find over levels (which is how your PH engine's H₀ works anyway; the paper's version space is just one such complex). One Lean structure, one stability theorem, two clients. This is a design decision worth making deliberately, and it's the only place the repos should share a definition.

**The density-of-states identity.** Toolkit statement, one proof; the paper contributes nothing to it except an instantiation, E := violation count. Any Potts system on an unweighted graph already satisfies the integer-energy hypothesis, so it was yours before the paper existed.

**An optional backport.** The quotient-aware sampling I described — propose in the full space, canonicalize, run the chain on normal forms — is paper-side wiring. But "RJMCMC engine accepts a user-supplied canonicalization hook" is a small, general API extension the paper _motivates_ and the toolkit _owns_, if you decide it's worth having. Take it or leave it; the engine is correct without it.

**The shared habit, not shared code:** the one-sided estimator discipline. Your commutativity Lean burdens and the paper's surrogate one-sidedness are the same principle applied in two places. That's methodology overlap, and methodology doesn't need a home in either repo.

# I actually have some other lean partial results that i kind of want to bring into perspective here

These slot in cleanly, and they change two things about what we built earlier — one for the merge-tree design, one for BARS. Audit first, then placement.

## The three files, and a note on the sorry count as a dashboard

The count (0 / 2 / 0) is misleading in a symmetric way: **PKWangA** is not sorry and not vacuous — the reduction proof is real. I walked the rewrite chain and it closes: `lt_sub_comm` moves the exponential across, `lt_log_iff_exp_lt` at 1/2, `log(1/2) = −log 2`, sign flip, `lt_div_iff₀`, done — the iff lands on syntactic identity. Legitimate, modulo Mathlib name drift. (One polish item: the `constructor` in the `loopless` block looks vestigial — `intro u h; exact h.1 rfl` should do it alone.) **PKWangB** is sorry and not vacuous — and your own comment about a sorried statement over sorried defs apologizing twice is exactly the discipline; deferring Lemma A's statement was right. **PottsGriffiths** is not sorry _and_ vacuous, for now: zero sorries because zero statements — an empty namespace under a rich header. It's the only actually vacuous object in the batch, which the dashboard inverts completely.

## Unblocking PKWangB: don't sort

Both sorried defs describe "sum of couplings ranked at or below this edge," and the `Finset.sort` obligation is self-inflicted — sorting is the C# implementation, not the spec. The spec-level object is a filtered sum:

```lean
noncomputable def localHCum (J : V → V → ℝ) (u v : V) : ℝ :=
  ∑ w ∈ Finset.univ.filter (fun w => J u w ≤ J u v), J u w
```

and `globalHCum` the same over pairs (via `Sym2` or an off-diagonal product set — a deliberate choice about double-counting either way). This dissolves both sorries in one move, demotes "sorted-prefix-sum equals filtered sum" to the implementation-correspondence lemma it actually is, and is _better-specified than the sorted version under ties_: `Finset.sort` with tied couplings is order-ambiguous, while the filtered sum includes the whole tie class deterministically. It also forces the diagonal convention (`w ≠ u`, or `J u u = 0`) into the open, where it belongs. Related tie point on the A side: `deterministicCutGraph` uses strict `<`, so edges exactly at threshold are cut — fine and measure-zero for continuous couplings, but the Lemma B part 2 statement must pick the matching single-linkage convention or the equivalence fails precisely at ties.

## PottsGriffiths: the chain, its scope, and a freebie

The chain the header promises has a standard skeleton: Edwards–Sokal coupling to the random-cluster measure; FKG positive association for q ≥ 1; the comparison inequality giving stochastic monotonicity in p; pull back through p = 1 − e^(−βJ). Conclusion: the two-point function is monotone in β. The scope matters and is the strength, not a limitation: this certifies monotone shape for _exactly_ the curves SPC thresholds — the pair correlations whose crossings define clusters — while χ and C are peaked by design and stay on the free-knot side. So BARS inherits a theorem-backed two-regime estimand taxonomy: provably-monotone observables get monotone splines, peaked observables get free knots and the merge-tree readout. Your "adversarial check" is presumably guarding one of the standard traps — q ≥ 1 (FKG fails below it), ferromagnetic couplings, boundary conditions. Note that the ferromagnetic hypothesis is _satisfied by construction_ in SPC — Gaussian-kernel couplings are positive — which deserves to be its own one-line lemma rather than an assumption. And one freebie to stop the namespace being empty today: `d⟨E⟩/dβ = −Var(E) ≤ 0` needs no Griffiths machinery at all — energy monotonicity is a one-line variance argument and can be the file's first proved statement while the FK chain is under construction.

## What this changes upstream

Two upgrades. First, **the merge tree gets a third client, from inside the toolkit.** Sweep T and the threshold T·log 2 rises through fixed H*cum values: `deterministicCutGraph` is a monotone-decreasing graph family, components splitting as edges drop out at their crossing temperatures. Lemma B part 2 — components equal single-linkage clusters — is precisely the statement that this family \_is* the single-linkage dendrogram, and single linkage is the H₀ merge structure (the Carlsson–Mémoli correspondence). So the answer to your TODO's "needs a formalized `singleLinkageClustering` to state against" is: don't build one — state it against the carrier-generic merge tree. That structure now has three instantiations — BARS curves over ℝ, the PKWang threshold filtration, and (paper-side) the violation filtration — with the first two entirely inside ThermoMapper, which settles the abstract-design question on toolkit grounds alone.

Second, **the monotonization has a compounding consequence for the readout**: once G*ij(T) is provably monotone, SP-2 applies globally, not per-bracket — each edge has a \_unique* threshold-crossing temperature, an exact pushforward through the BARS posterior. The per-edge splitting temperatures are then well-defined random variables, and the PKWang dendrogram over T is their single-linkage structure. That's the Griffiths chain, the crossing lemma, and the solver reduction composing into one pipeline claim: the cluster hierarchy is an exactly-computable functional of monotone posterior curves. I'd also read your `Enthymemata.Bifiltration` pointer in this light: monotonicity is what makes the temperature axis an honest filtration direction at all — without it, T-indexed super-level families are non-nested and you're in zigzag territory. Same pattern as the paper's oracle-monotone/learned-zigzag split, but shared as a pattern only; no code crosses.

## Placement

All three files are ThermoMapper by the litmus test — no carrier, congruence, evidence, or residual anywhere — and they sit _below_ BARS.lean in a now fairly clean layer stack: PottsGriffiths certifies what shape the estimand provably has; PKWang certifies what the solver computes; BARS.lean certifies what you may read off fitted curves; the merge tree is the N-free object the readouts land in. The paper touches none of it except through adapters. The single candidate paper-side echo, flagged as speculative and nothing more: the Boolean version-space Gibbs measure is a product over observed coordinates, hence trivially positively associated — but conditioning on expressibility breaks product structure, and whether FKG survives that conditioning is an open adapter-level question, not a reason to entangle the repos. One housekeeping thought: the revised BARS.lean's proved/sorried split maps directly onto your `Lemmas.*` / `Enthymemata.*` convention if you want to fold it into the same layout.

# this is tangential to the paper, but the pkwang second result is what i've been leaning towards as the mapper clusterer backend instead of full-blown SPC. the local-field solver might be good enough for the mapper pullback clustering, which effectively filters off alot of the topological frustration so perhaps the simplification sits well there

I'd push your framing one notch stronger: this isn't a defensible simplification, it's the theoretically sanctioned choice, and full SPC per bin would actually be the deviation. Mapper's clustering step has a defined estimand — π₀ of the pullback, connected components — and the Reeb-convergence results for Mapper (the Carrière–Oudot line, and the statistical parameter-selection work built on it) are proved for exactly the single-linkage-at-scale family, because that family _is_ the component estimator. SPC per bin does something more than π₀: it splits connected preimages at density bottlenecks, which targets a density-Reeb hybrid rather than the Reeb graph of your filter. If "Reeb graphs analytically" is the pillar's target — and you said it is — then the PKWang backend isn't good enough, it's aligned, and the guarantees you'd want to inherit only exist on that side.

Your frustration intuition also has a precise form worth writing down, because it tells you exactly what the pullback does and doesn't remove. Over a filter interval containing no critical value, the preimage is Morse-cylindrical — it deformation-retracts to a level set, component structure constant across the bin. Loops get cut into contractible arcs whose gluing the nerve recovers; along-filter multimodality lands in different bins. So the pullback moves topological complexity out of the clustering problem and into the nerve combinatorics _by design_ — that's the mechanism under your "filters off frustration." And it dovetails with the BARS-cover proposal from before in a way that makes the two decisions one decision: covers anchored on posterior-located critical values, with criticals placed in the overlaps, make every pure bin a critical-free slab — precisely the regime where per-bin clustering degenerates to level-set component estimation, the easiest version of the problem. What the pullback does _not_ remove: transverse density gaps (which are legitimately the clusterer's job) and noise chaining (single linkage's classic failure). Two terminology cautions for the prelemma docs: strictly, ferromagnetic SPC has no frustration at all — mixed-sign loop couplings are absent by construction — so the precise claim is "along-filter topological complexity," and a physics-literate reader will hold you to that.

The chaining residual is where the _local-field_ half of your choice earns its keep, and it places PKWang in better company than you may have noticed. Density-adaptive per-site ranking is a locally renormalized dissimilarity — structurally the same move as HDBSCAN's mutual-reachability transform, which is exactly what turns naive single linkage into robust single linkage. And robust single linkage is the one clustering family with consistency theorems for the density cluster tree (the Chaudhuri–Dasgupta line). So your backend lands in the unique intersection: Mapper-side Reeb convergence for the single-linkage family, cluster-tree-side consistency for the density-adaptive variant. That also promotes Lemma A from housekeeping to the control theorem of the whole backend: the pooling limit characterizes when local ranking ≈ global ranking, homogeneous critical-free bins are exactly the regime where the pooled limit is benign, and Lemma A + Lemma B part 2 together become the correctness story — per-bin PKWang-local is density-robust single linkage whose components estimate the pullback's π₀.

Three operational payoffs, briefly. Determinism composes with your exact-pushforward philosophy: across volley refits of the cover, a Monte Carlo clusterer injects seed variance into the nerve that the posterior machinery would then have to model; the deterministic cut leaves randomness only where it belongs, in the BARS posterior. Cost: the per-bin _dendrogram_ is one Kruskal pass — sort the fixed H_cum ranking once, and the entire T-sweep is a walk down the sorted list, since T·log 2 just moves through a merge tree computed once; that's the third merge-tree client from last turn made operational. And per-bin scale selection gets the stability-backed rule for free: cut each bin's dendrogram at the largest persistence gap, which is precisely the thresholded-count sandwich from BARS.lean applied per bin.

Where full SPC stays load-bearing, so the division of labor is clean: it remains the _measurement_ engine — thermal expectations, ⟨δ⟩, χ, the curves BARS fits are ensemble averages a deterministic partition solver cannot produce — while PKWang becomes the _partition_ engine. And I'd keep SPC in the Mapper loop in one capacity: auditor. Per-bin disagreement between the cheap and full clusterers is a direct cover-quality diagnostic — agreement confirms the bin is in the benign regime, disagreement localizes exactly the bins where the pullback failed to remove the difficulty — which gives the volley refinement a second, structural acquisition signal alongside the curve posteriors. Refine where cheap and expensive disagree.

One sentence on the paper, since you flagged the tangent: a deterministic Mapper backend means that if the version-space Reeb taxonomy ever runs through this pipeline, the oracle is seed-free and exactly reproducible — the same design instinct as your model-free benchmark oracle, now holding at the toolkit layer too.

# ok well here are my remaining "enthymemata" including the bifiltration one. Taken all together, what are your proposed extensions/additions/enhancements/fixes across the set? As you can see from the collection i haven't touched Mapper at all in my lean work yet, but something i've wanted to dig into but unable to see the connections to other pillars in terms of formalisms

Reading the three together with the four from before, the estate has a shape now, and Stability.lean is the file that reveals it — so let me audit briefly, then give you the cross-set proposals, with the Mapper answer as the centerpiece, because it turns out Mapper isn't a missing pillar needing new formalism. It's one definition you don't have yet, of which three of your existing modules are already instances.

## Quick audit

**Stability.lean is the best-architected file in the estate.** The pattern — opaque stand-ins, exactly one cited AXIOM (the function-stability theorem), then genuinely proved plumbing — is right, and I checked both proofs term-by-term: `stability_event_subset` and `confidence_pushforward` are correct as written, and your use of `measure_mono` quietly dodges all measurability obligations because Mathlib's measures are monotone on arbitrary sets. That's either luck or taste; either way keep it. Two fixes while everything is still opaque, because both are cheap now and expensive later. First, **thread tameness through the axiom**: CSEH-style stability at the _diagram_ level requires tame functions (module-level interleaving stability holds for all functions, but diagrams don't exist without q-tameness), so add an opaque `Tame : (ℝ → ℝ) → Prop`, hypothesize it in the axiom, and thread it through both theorems. The payoff is a cross-file gift: **BARS draws are piecewise polynomial, and BARS.lean's finiteness lemmas (MP-1, SP-1) are precisely the tameness certificate** — the hypothesis your axiom needs is discharged by lemmas you already have. Second, note in a comment that `supNorm` must become sup over the thermal window `Icc`, not ℝ, when de-opaqued. Also: `landscape_stable`'s sorry is a different species from every other sorry in the estate — over all-opaque types it is _unprovable in principle_ until `PersistenceLandscape` is a def, whereas BARS sorries are dischargeable today. Either promote it to AXIOM under your own stated policy (it's a cited literature fact, same status as CSEH) or move the statement into the TDA.Ph stub.

**Bifiltration and Ascent are header-only** — targets, not enthymemata proper — and I'll respect the do-not-restate-from-memory guard in Ascent's header and not fabricate L1–L5; what I'll propose for both is interface structure only. One note with some satisfaction: your protolemma's correction that "Griffiths is the wrong tool, it's Fortuin–Kasteleyn" is the same correction I'd have made — the chain I sketched two turns ago _was_ the FK route (Edwards–Sokal, FKG for q ≥ 1, comparison inequalities), so the file `PottsGriffiths` is now a misnomer by your own analysis; rename to `PottsFK` or note it.

**Estate-wide fix #1, the dashboard.** Across seven files the sorry counts read 6/0/2/0/0/1/0 and mean six different things: dischargeable-today, honest-def-debt, empty-namespace, and unprovable-until-a-layer-exists all print the same. Adopt the taxonomy your files are already groping toward and put it in every header: **PROVED / AXIOM (cited, never to be formalized) / OBLIGATION (statement real, proof pending) / TARGET (header only)**. Stability.lean already does this; make it law.

## The keystone: one functor, four diagram shapes — and that's Mapper

Here is the connection you couldn't see, and it's smaller than you feared. Define, once, **the connected-components functor**: Mathlib gives you the complete lattice on `SimpleGraph V`, `Hom.ofLE : G ≤ H → G →g H`, and `ConnectedComponent.map` along homs — compose and you have `π₀ : (subgraph poset) ⥤ Type`, plus the vertex-subset variant via `induce` and inclusion homs. Fully combinatorial, no topology, maybe forty lines. Call it `Pi0.lean`. Then:

- **A chain** T ↦ `deterministicCutGraph T` under π₀ _is_ the merge tree — and PKWangB's Lemma B part 2 should be stated against exactly this, which resolves your TODO's "needs a formalized `singleLinkageClustering`": don't build one; single linkage on the threshold chain is this functor by definition-as-theorem.
- **A product poset** (two thresholds) under π₀ is the H₀ bifiltration — Bifiltration.lean's combinatorial spine.
- **A zigzag shape** under π₀ is the H₀ backbone your PH engine's zigzag work needs.
- **A cover's intersection poset** under π₀, nerve-glued, **is Mapper.** Vertices are `Σ_a π₀(induce (f ⁻¹' U_a))`; edges come from the maps out of `π₀(f ⁻¹' (U_a ∩ U_b))`. With the PKWang backend decision, the per-bin clusterer is `π₀ (deterministicCutGraph.induce (f ⁻¹' U))` — a formal object you already possess, with zero probabilistic machinery to formalize.

And the first Mapper theorem is already reachable and welds the pillar to the others on day one: **Mapper on a nested (sublevel) cover degenerates to the merge tree.** One lemma, provable from the functor's functoriality alone, and it makes "Mapper" a formalized pillar whose first result is a statement _about_ the PKWang track. The discrete Reeb graph of (G, f) is then the fine-cover limit — a target, not a prerequisite.

## Per-file extensions

**PKWangA — generalize the reduction to arbitrary θ.** Your proved theorem is the θ = 1/2 slice of something: `1 − exp(−H/T) > θ ↔ H > T · log (1/(1−θ))` for θ ∈ (0,1), with the identical proof modulo one constant. This matters structurally: it separates the estate's **two bifiltrations**, which your ideal-vs-estimate landmine note shows you already care about. The _solver_ bifiltration (T, θ) on H*cum is monotone in both parameters by arithmetic — no physics needed. The \_estimand* bifiltration (β, θ) on ideal correlations needs FK for the β-direction. PKWang realizes slices of the first; the FK chain certifies the second; Lemmas A and B are the bridge between them. Conflating the two is exactly the landmine.

**PKWangB — the filtered-sum defs** from before (kill both sorries, better-specified under ties, forces the diagonal convention into the open), plus Lemma A restated as the pooling limit with an explicit homogeneity hypothesis — its promoted role as the Mapper-backend control theorem.

**PottsFK — copy Stability.lean's architecture to unblock three files at once.** The FK chain's deep input (the comparison inequality) deserves the same treatment CSEH got: AXIOM with the Grimmett citation over light structures, then _prove_ the plumbing — pullback through p = 1 − e^(−βJ) — to deliver `spin_agreement_mono` as a usable statement now. Bifiltration is currently blocked on a theorem you never intended to formalize deeply; the axiom policy exists precisely for this. Add the two freebies so the file exits TARGET status today: `d⟨E⟩/dβ = −Var ≤ 0` (one line, no FK) and ferromagnetism-by-construction for Gaussian-kernel couplings.

**Bifiltration** — with `spin_agreement_mono` available even as axiom-plus-plumbing, the spine is: the (β, θ) edge family is doubly monotone, hence a genuine bifiltration, hence the π₀ functor applies on the product poset. And the landmine gets a formal home rather than a warning comment: the theorem says the _estimand_ is in the monotone class; the BARS monotone fit is in the class _by construction_; no claim is ever made about the raw estimate — the sup-norm band between two class members is what Stability.lean's pushforward consumes. Your C_v warp-vs-slope note suggests one small shared module: monotone-reparameterization invariance of barcodes (endpoints transform, thresholded counts invariant), which also serves Ascent's L4 gauge item.

**BARS.lean — one revision to the earlier plan, for coherence.** The thresholded-count stability sandwich I proposed shouldn't be proved standalone: it's the H₀ corollary of `diagram_stability` once tameness is threaded, so it belongs downstream of Stability.lean. The instability lemma (raw count explodes under ε-wiggle) stays BARS-local. The merge-tree structure moves into `Pi0.lean` as the chain instantiation — which was the carrier-generic design decision anyway, now with four clients.

**Ascent** — interface proposal only, honoring your guard: state the ascent forest over the same `SimpleGraph` + vertex-function data the π₀ functor consumes, so that basins and merge-tree components live in one vocabulary and the L5 separation (capture ≁ ascent) can be a theorem _comparing two functors on the same diagram_ rather than a statement about two unrelated formalizations. That's also the shape under which a ToMATo-style reconciliation (persistence-merged basins = components at threshold) becomes stateable later.

## Build order

1. `Pi0.lean` — unblocks Lemma B pt 2's statement, Mapper's definitions, the merge tree, and Bifiltration's functoriality in one stroke.
2. PKWangB filtered sums + PKWangA θ-generalization — quick, independent.
3. PottsFK in the Stability architecture → `spin_agreement_mono` → Bifiltration spine.
4. Stability tameness retrofit + estate-wide status taxonomy.
5. BARS remaining sorries per the existing routes; sandwich re-derived downstream.
6. `Mapper.lean` — definitions plus the nested-cover ≡ merge-tree theorem.
7. Ascent from its prelemma, with the basin/component comparison as the integration target.

If you want artifacts next, the natural set is `Pi0.lean`, the θ-generalized PKWangA, and the tameness-threaded Stability patch — the first is the one worth designing carefully, since at this point everything else in the estate hangs off it.
